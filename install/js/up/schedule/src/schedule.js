import {Type} from 'main.core';

export class Schedule
{
	constructor(options = {name: 'Schedule'})
	{
		this.name = options.name;
	}

	setName(name)
	{
		if (Type.isString(name))
		{
			this.name = name;
		}
	}

	getName()
	{
		return this.name;
	}
}